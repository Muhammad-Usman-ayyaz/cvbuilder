GEMINI_API_KEY=
PORT=8001
# Optional — defaults to gemini-3.6-flash if unset. Gemini model names get
# deprecated over time (confirmed live: gemini-2.5-flash was rejected with
# "no longer available to new users" as of 2026-08-27) — verify this is
# still current before relying on it.
GEMINI_MODEL=gemini-3.6-flash
